алг сложный_пример;
нач
    х : цел;
    у : цел;
    счетчик : цел;
    флаг : лог;
    массив : таб[10] цел;
кон
    х := 5;
    у := х + 3 * 2;
    флаг := х > у;
    
    если флаг то
        вывод("х больше у");
    иначе
        вывод("х не больше у");
    все
    
    для счетчик от 1 до 5
        массив[счетчик] := счетчик * 2;
        вывод("элемент");
    кц
    
    пока х < 10
        х := х + 1;
    кц
кон 