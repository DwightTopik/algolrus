алг сложная_математика;
нач
    a : цел;
    b : цел;
    c : цел;
    флаг : лог;
кон
    a := 10;
    b := 5;
    c := a + b * 2 - 3 / 1;
    флаг := a > b и c < 20;
    c := (a + b) * (c - a);
кон 